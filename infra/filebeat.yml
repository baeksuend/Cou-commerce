# filebeat.yml 예시
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/filebeat/logs/*.log
    fields:
      service: cou-commerce

output.logstash:
  hosts: [ "logstash:5044" ]  # Logstash 호
