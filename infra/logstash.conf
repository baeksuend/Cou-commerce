# logstash.conf 예시
input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "cou-commerce-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }  # 로그 확인용
}